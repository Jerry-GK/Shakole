This is a search engine named "Shakole", which can search works of Shakespeare.
It's implemented by reverted file index, using AVL tree to index.

To run:
method 1:
Enter "g++ src\\*.cpp -o bin\test.exe" in terminal and run test.exe in bin.

method 2:
Enter "make" in terminal and run test.exe in bin.